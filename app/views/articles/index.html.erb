<% unless @articles.empty? %>
<div id="article">
  
  <h1 class="title"><%= link_to @articles.first.title, "/articles/#{@article.to_param}" %></h1>

  <div class="body">
    <%= @articles.first.body_html %>
  </div>
  
</div>
<% else %>
  <h2>There are no articles</h2>
<% end %>